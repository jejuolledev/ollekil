// ============================================
// 로컬 설정 파일 (템플릿)
// ============================================

// 사용 방법:
// 1. 이 파일을 config.js로 복사
// 2. GITHUB_TOKEN에 실제 토큰 입력
// 3. config.js는 .gitignore에 있어서 Git에 커밋되지 않음

export const CONFIG = {
  // GitHub Personal Access Token
  // https://github.com/settings/tokens/new?description=Ollekil%20Blog%20Image%20Upload&scopes=repo
  GITHUB_TOKEN: 'YOUR_GITHUB_TOKEN_HERE', // 실제 토큰으로 교체
};
